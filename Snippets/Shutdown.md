```powershell
shutdown /a                # Cancel Pending Shutdown
shutdown /r /f /t 0        # Reboot system
shutdown /r /fw /f /t 0    # Restart to BIOS
shutdown /s /t 0           # Shut Down Local Computer
```